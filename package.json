{
  "name": "epicast-demo",
  "version": "1.0.0",
  "description": "Server and client for the epicast demo",
  "author": "Rick Hawes <rhawes@cdc.gov>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "concurrently": "^7.4.0"
  },
  "scripts": {
    "install-all": "cd client && yarn install && cd ../server && yarn install",
    "start": "concurrently \"yarn run server\" \"yarn run client\"",
    "server": "cd server && yarn start",
    "client": "cd client && yarn start"
  },
  "dependencies": {
    "date-fns": "^2.29.3"
  }
}
