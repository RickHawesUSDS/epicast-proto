{
  "name": "epicast-demo",
  "version": "1.0.0",
  "description": "Server and client for the epicast demo",
  "author": "Rick Hawes <rhawes@cdc.gov>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "concurrently": "^7.4.0"
  },
  "scripts": {
    "install-all": "cd website && yarn install && cd ../service && yarn install",
    "start": "concurrently \"yarn run service\" \"yarn run website\"",
    "service": "cd service && yarn start",
    "website": "cd website && yarn start"
  },
  "dependencies": {
    "date-fns": "^2.29.3",
    "yaml": "^2.1.3"
  }
}
