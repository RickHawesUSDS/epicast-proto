import { TimeSeriesMutator } from "@/models/TimeSeries";
import { FeedBucket } from "@/models/FeedBucket";
import { FeedSchema } from "@/models/FeedSchema";
import YAML from "yaml";


export async function readSchema(fromBucket: FeedBucket, updateTimeSeries: TimeSeriesMutator): Promise<void> {
  if (updateTimeSeries.)
  const hasUpdate = lastSchema(fromBucket: FeedBucket)
  const newSchema: FeedSchema = parse()

}
