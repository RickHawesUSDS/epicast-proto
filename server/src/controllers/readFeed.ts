import { Bucket } from "@/models/Bucket";
import { TimeSeriesMutator } from "@/models/TimeSeries";

export async function readFeed(fromBucket: Bucket, timeSeries: TimeSeriesMutator ): Promise<void> {

}
